// this barrel file is used when the apps need to call hooks from wagmi

import {
  useReadContracts,
  useReadContract,
  useBalance,
  useWaitForTransactionReceipt,
  useWriteContract,
  BaseError,
} from 'wagmi'

export {
  useReadContracts,
  useReadContract,
  useBalance,
  useWaitForTransactionReceipt,
  useWriteContract,
  BaseError,
}
